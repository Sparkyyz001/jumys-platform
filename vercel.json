{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && npm install --prefix nextjs",
  "buildCommand": "npm run build --prefix nextjs && rm -rf .next && cp -R nextjs/.next .next",
  "devCommand": "npm run dev --prefix nextjs"
}
